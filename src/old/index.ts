const createInsertHandler = require('./insert');
const createRemoveHandler = require('./remove');
const createSearchHandler = require('./search');
const createUpdateHandler = require('./update');

export default {
  createInsertHandler,
  createRemoveHandler,
  createSearchHandler,
  createUpdateHandler,
};
